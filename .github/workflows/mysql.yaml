name: Run Mysql Example
on: push

jobs:
  mysql-schema-initialization:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: shogo82148/actions-setup-mysql@v1
        with:
          mysql-version: '8.0'
      - run: |
          mysql -uroot -hlocalhost -e 'SELECT version();';
          systemctl start mysql;
          mysql -uroot -hlocalhost < .github/setups/schema.sql;
          mysql -uroot -hlocalhost -e 'use customer; Show tables;';





